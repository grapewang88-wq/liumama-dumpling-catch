# 刘妈妈「接馅料 · 包饺子」

C1 售货机营销小游戏第三款。20 秒限时，按左右键挪动饺子皮，接住掉下来的食材，凑齐配方就包成一只饺子，结束出分数并进本机排行榜。

**这一款不接出货、不联网**，纯前端单文件，双击 `index.html` 就能玩。

---

## 一、玩法

| | |
|---|---|
| 单局时长 | 20 秒（运维面板里 10~60 秒可调） |
| 操作 | 屏幕底部左右两个大按钮，**按住不放就一直挪**，松手就停。这是唯一的操作方式，不支持拖动 |
| 目标 | 顶部目标卡显示当前要包的馅，下面列出需要的食材和个数 |
| 接对 | 该接的食材：加分、进度 +1 |
| 接错 | 不该接的食材：扣 15 分（分数不会扣成负数）、连击清零 |
| 成饺子 | 配方凑齐 → 饺子飞出去 + 大分 → 自动换下一个馅 |
| 漏接 | 该接的没接到：不扣分，但连击断掉 |
| 硬币 | 5% 概率掉落的 1 元硬币，+60 分，接到不影响连击，漏掉也不扣 |

**待机页会把这四条规则直接列出来**（接对 +10 起 / 包成 +100 起 / 硬币 +60 / 接错 −15），玩家不用先玩一局才知道硬币是干嘛的。改分值时记得同步改那一条，位置在 `index.html` 的 `<div id="rules">`。

**换馅免罚窗口**：刚包成一只饺子的 0.8 秒内，天上还飘着上一个配方的食材，这时候接到它们不扣分，只提示「换馅啦」。这是刻意留的，否则玩家会被上一轮的余料坑，体感很差。

### 六种馅 = 六个配方

| 馅 | 需要的食材 | 稀有度倍率 |
|---|---|---|
| 韭菜双虾 | 韭菜×2 + 虾仁×1 | 1.0 |
| 马蹄玉米鲜肉 | 玉米×2 + 鲜肉×1 | 1.0 |
| 茴香鸡蛋 | 茴香×2 + 鸡蛋×1 | 1.1 |
| 萝卜丝脂渣 | 萝卜×2 + 脂渣×1 | 1.2 |
| 胶东鲅鱼 | 鲅鱼×2 + 鲜肉×1 | 1.4 |
| 八蛸干贝 | 干贝×2 + 八蛸×1 | 1.6 |

「鲜肉」被鲅鱼和玉米两个配方共用——这是故意的，制造一点「这个到底该不该接」的判断。

### 算分

```
接对一个食材   = 10 × (1 + 0.15 × 连击数)   连击最高按 8 算，也就是最多 22 分
包成一只饺子   = 100 × 稀有度倍率           （100 ~ 160 分），并且连击 +2
接错一个       = -15 分，连击清零
1 元硬币       = +60 分
```

模拟跑分（12 局取平均，AI 陪练只会按左右键，和真人条件一样）：

| 水平 | 平均分 | 平均包成 | 接对 | 接错 |
|---|---|---|---|---|
| 高手（几乎不漏） | 626 | 3.5 只 | 10.8 | 5.7 |
| 一般 | 507 | 2.9 只 | 9.7 | 5.8 |
| 手生 | 475 | 2.7 只 | 8.6 | 6.5 |

注意「接错」这一列：改成左右键之后，皮是**扫**过去的，路上会顺手接到不该接的东西。所以「什么时候忍住不扫」本身成了一个技巧点，比拖动版更考验判断。

真人第一次玩大概 200~400，熟手 600~900。**要整体调难度，改运维面板里的「难度」滑杆就行**（0.7 慢 ~ 1.4 快，同时影响掉落速度和出现频率）。

---

## 二、排行榜

存在浏览器 `localStorage`，key 是 `c1_catch_v1`，**只在这台机器上**。

- 结算页自动生成一个随机名号（「面粉小当家」这种），玩家可以点「换一个」洗，也可以直接点输入框改成自己想要的（最多 6 个字）
- 榜单页分「今日榜」和「总榜」两个 tab，各显示前 20 名，本人那一行会高亮
- 最多保留 300 条，超了自动丢最低分的
- 运维面板可以清今日榜 / 清全部 / 导出 JSON

### 以后要做联网榜单

现在所有读写都收在 `index.html` 第 2 节的 `Store` 对象里，联网只要改两个地方：

```js
Store.add(rec)        // 一局结束后提交：{n:名字, s:分数, d:饺子数, c:最高连击, a:准确率, t:时间戳}
Store.list(scope)     // 取榜单：scope 是 "today" 或 "all"，返回按分数降序的数组
```

把这两个换成 `fetch` 你们的接口即可，其它代码不用动。建议保留本地写入做兜底：网络挂了至少本机榜还在，玩家不会看到空榜。

---

## 三、素材

目录里只留了这一款真正用到的文件，没有死素材：

```
assets/
  bg-attract.png bg-game.png           待机页 / 游戏页背景（复用「闻馅识饺子」）
  btn-primary[-press].png              主按钮（复用）
  btn-ghost[-press].png                次级按钮（复用）
  card-9slice.png                      卡片九宫格框（复用）
  dumpling-glow.png                    成饺子时的光环（复用）
  icon-hand-tap.png                    待机页玩法图示的手（复用）
  icon-sound-on/off.png                声音按钮（复用）
  fillings/filling-<馅id>.png           六种馅的饺子 600×480（复用）
  flavors/flavor-title-<馅id>.png       六种馅名艺术字 1200×300（复用）
  items/item-<食材id>.png               11 种食材 160×160            ← 本作新做
  items/item-coin.png                   1 元硬币（奖励物）160×160    ← 本作新做
  title-catch.png                       待机页标题艺术字 1012×222   ← 本作新做
  title-board.png                       排行榜标题艺术字 734×196     ← 本作新做
  items/fx-sparkle.png                  接到时的小星星              ← 本作新做
  skin.png / skin-shadow.png            饺子皮 320×160 和它的影子    ← 本作新做
  bgm.mp3  sfx/*.mp3|wav                音乐和 6 个音效（复用）
```

**新素材是脚本生成的**：`python3 素材加工.py` 会重新生成 `items/`、`skin.png`、`skin-shadow.png`、两张标题艺术字。

- 食材和饺子皮：高倍超采样画形状 → 降到 40×40 像素网格 → 加 1px 深棕描边 → 整数放大 4 倍
- 标题艺术字：字体小尺寸渲染 → 二值化保证硬边 → 分层描边（深棕粗边 + 金色细边 + 硬投影）→ 整数放大 2 倍

两种做法出来的都是真像素画，和原有的饺子、馅料名艺术字放一起不违和。标题要改字，改 `titles()` 里的文案重跑脚本即可。

要**换成美术手绘的正式素材**：直接覆盖同名文件即可，尺寸保持 160×160（食材）和 320×160（饺子皮），保持透明底。代码只按文件名找图，不认内容。食材 id 见 `index.html` 里的 `FOODS`。

---

## 四、界面

四屏，都按 1080×1920 手排的，没有用现成 UI 框架。

| 屏 | 内容 |
|---|---|
| 待机 | 品牌行 → 标题艺术字 → 副标题绶带 → 三步玩法卡 → **计分规则条** → 本机最高分牌 → 开始/排行榜按钮 |
| 游戏 | 顶部三块 HUD（得分/剩余/连击，连击 ≥3 时右上角冒出倍率角标）→ 像素分段计时条 → 目标卡（左边馅名艺术字，右边缺什么、还差几个）→ 掉落区 → 饺子皮 → 底部左右大键 |
| 结算 | 名次绶带（前三名镀金/银/铜）→ 分数滚动 → **这一局包出来的饺子实物摆出来** → 三项统计 → 起名 → 再来一局/看榜 |
| 榜单 | 标题艺术字 → 今日榜/总榜切页 → 前三名奖牌 + 本人行高亮打「你」标 → 不足十条补虚位 |

视觉上统一的几条：

- **字号只用四档**：52 强调 / 32 正文 / 26 标签 / 数字单列一档，中文一律加字距
- **颜色三档**：`#4E2317` 主文字、`#A9714A` 次要标签、`#C0392B` 强调，绿色 `#1E7A3C` 只用于加分
- **投影是硬的**（`0 10px 0` 这种实心偏移），不是柔和阴影 —— 像素风里柔和阴影会显脏
- 每屏都压了一层暗角，浅色背景上的白卡片才不会糊在一起；待机页顶部另压了一层夜色，把背景那块大招牌推到标题后面去

---

## 五、运维面板

**待机页左上角长按 3 秒**打开。可调：

- 声音总开关 / 背景音乐音量 / 音效音量
- 单局时长、难度倍率
- 结算页无操作多少秒自动回待机（默认 45 秒）
- 清榜、导出榜单 JSON

所有改动都写进 localStorage，重启页面仍然生效。

---

## 六、上机注意

1. **分辨率**：按 1080×1920 竖屏画的，代码会自动等比缩放铺满，非 9:16 的屏两边留黑边，不会变形。
2. **触摸**：只用了 touch 事件，不依赖鼠标；已禁用长按菜单、双指缩放、双击放大。两个键同时按会互相抵消（皮不动），松开其中一个立刻跟着另一个走——老人小孩容易两只手一起按，这个情况照顾到了。键盘方向键也能用，机器如果带实体按键可以直接接上。
3. **声音**：浏览器策略要求先有一次触摸才能出声，代码已经挂了「首次触摸自动起 BGM」。如果机器上是静音壳，直接在运维面板关掉总开关。
4. **息屏/切后台**：页面不可见时游戏会自己停住（计时也停），切回来接着走，不会出现「后台跑完了 20 秒」的情况。
5. **常年开机**：结算页和榜单页无操作 45 秒自动回待机，不会有人把机器停在别人的成绩页上。
6. **⚠️ 排行榜要能存下来，WebView 必须打开 DOM storage**：

   ```java
   WebSettings ws = webView.getSettings();
   ws.setDomStorageEnabled(true);      // 不开这个，榜单每次重启都是空的
   ws.setAllowFileAccess(true);        // 用 file:///android_asset/ 加载时需要
   ```

   iOS 的 WKWebView 默认就支持，不用额外设置。如果 storage 被禁用，游戏本身照常能玩，只是分数存不下来——代码里做了容错，不会白屏或报错。

---

## 七、这一款和前两款的关系

| | 闻馅识饺子 (`c1-game`) | 煮饺子秘密特训 (`c1-game-boil`) | 接馅料包饺子（本款） |
|---|---|---|---|
| 玩法 | 认馅 | 掐火候 | 反应 + 手速 |
| 时长 | 多轮 | 多轮 | 20 秒一局 |
| 出货 | 接 | 接 | **不接** |
| 排行榜 | 无 | 无 | **有（本机）** |

三款共用同一套背景、按钮、九宫格、音效和饺子素材，风格是统一的。前两款是「玩通关 → 掉实物」，这一款是「刷分 → 上榜」，适合放在人多的场合冲人气；如果之后要给它也加中奖出货，把前两款的 `Bridge` 那一节整段拷过来，在结算处按分数线触发即可。
