# 「接馅料 · 包饺子」素材清单

给品牌美工重做全套素材用。配套看 `docs/素材总览.jpg`（现有素材长什么样，一张图全在里面）。

---

## 零、三条铁律

**1. 文件名必须一模一样。** 代码只按文件名找图，不认内容。`item-chive.png` 改成 `韭菜.png` 就找不到了。

**2. 画布尺寸必须一模一样。** 下面每一项都写死了尺寸。尺寸变了要么被拉变形，要么错位。**确实需要改尺寸的，先告诉我，我改代码里对应的数值**，不要自己改了直接交。

**3. 除了两张背景，全部要透明底。** PNG-24 带 alpha 通道，不要白底，不要预乘 alpha。

做到这三条，美工交付的包直接覆盖 `assets/` 目录就能上线，不用改一行代码。

---

## 一、交付规范

| 项 | 要求 |
|---|---|
| 格式 | PNG-24（带 alpha）。背景两张可以是 PNG-24 不带 alpha |
| 色彩空间 | sRGB。不要 Display P3，会在售货机屏上偏色 |
| 目录结构 | 保持现有结构：`assets/` 下的 `items/`、`fillings/`、`flavors/`、`sfx/` 子目录不要动 |
| 画风 | 现在是**像素画**，代码开了 `image-rendering: pixelated`（不做平滑，保证像素块锐利）。如果新素材改成手绘/矢量风，告诉我，我关掉这个属性——**不关的话平滑插画会显得毛糙** |
| 像素画的话 | 建议按「小尺寸画 → 整数倍放大」做。现在食材是 40×40 网格放大 4 倍，所以每个像素是 4×4 的方块 |
| 交付方式 | 按原目录打包 zip 即可，不需要源文件（PSD 留着你们自己存档） |

**设计基准是 1080×1920 竖屏**，投在 32 寸电容触摸屏上，玩家站着看，距离约 50～80cm。所以：**宁可粗，不要细**。细线条和小字在这个距离上会糊掉。

---

## 二、素材清单

### 1. 下落食材 —— 12 张，全部 160×160

**这一组最重要。** 玩家要在食材下落的一两秒内认出它是什么，认错就扣分。所以每一张的要求是：**远看一秒内能认出来**。

| 文件名 | 内容 | 备注 |
|---|---|---|
| `items/item-chive.png` | 韭菜 | 三根一束，深浅绿区分 |
| `items/item-shrimp.png` | 虾仁 | 粉橙 C 形 |
| `items/item-corn.png` | 玉米 | 带绿叶的玉米段 |
| `items/item-pork.png` | 鲜肉 | 五花肉，红白分层 |
| `items/item-fennel.png` | 茴香 | 羽状叶 |
| `items/item-egg.png` | 鸡蛋 | 煎蛋，白+黄芯 |
| `items/item-mackerel.png` | 鲅鱼 | 蓝背银肚 |
| `items/item-octopus.png` | 八蛸（小章鱼） | 紫粉色 |
| `items/item-scallop.png` | 干贝 / 扇贝 | 米黄扇形 |
| `items/item-radish.png` | 白萝卜 | 白身绿缨 |
| `items/item-zhizha.png` | 脂渣 | 金褐色酥块 |
| `items/item-coin.png` | **1 元硬币**（奖励物，不是食材） | 要一眼区别于食材，现在是金色带「1元」字样 |

要点：

- **每张之间必须好区分**。同屏最多 8 个一起掉，颜色和外形撞了玩家就会接错
- 主体居中，四边留 8～10px 空隙 —— 下落时有 ±13° 的轻微摇摆，贴边会被切
- 建议保留深色描边（现在是 4px 的深棕 `#3A2116`）。背景是米黄天空，浅色食材不描边会糊进去
- 掉落时按 **160×160 原尺寸**显示（1:1，像素不糊）；待机页和目标卡上会缩到 74～112 显示

### 2. 饺子皮和特效

| 文件名 | 尺寸 | 内容 | 显示 |
|---|---|---|---|
| `skin.png` | **320×160** | 接馅料用的饺子皮，略俯视的扁圆 | 原尺寸 1:1 |
| `skin-shadow.png` | **320×60** | 皮下面的投影，虚化椭圆 | 原尺寸 1:1 |
| `items/fx-sparkle.png` | **96×96** | 接对时迸出的小星星 | 72×72 |
| `dumpling-glow.png` | **700×700** | 包成一只时背后的光环，中心透明 | 原尺寸 1:1 |

⚠️ **饺子皮有个隐藏约束**：代码的接取判定线在「贴图顶部往下 50px」处，也就是现在这张图里**皮面**所在的高度。如果新皮的造型让皮面明显上移或下移，告诉我，我改 `CATCH_LINE` 那个数字，否则会出现「看着接到了却没算」。

### 3. 六种馅的饺子 —— 6 张，全部 600×480

破口朝上、露出馅料的一只饺子。包成时飞出来，结算页会把这一局包出来的几只并排摆出来。

| 文件名 | 馅 |
|---|---|
| `fillings/filling-chive-shrimp.png` | 韭菜双虾 |
| `fillings/filling-corn-pork.png` | 马蹄玉米鲜肉 |
| `fillings/filling-fennel-egg.png` | 茴香鸡蛋 |
| `fillings/filling-radish-zhizha.png` | 萝卜丝脂渣 |
| `fillings/filling-mackerel.png` | 胶东鲅鱼 |
| `fillings/filling-octopus-scallop.png` | 八蛸干贝 |

- 六只的**饺子皮造型要一致**，只有露出的馅不同 —— 这样并排摆出来才像一锅的
- 显示尺寸从 400×320 到 124×99 不等，所以馅料的颗粒不要太碎，缩小后会糊成一坨

### 4. 六种馅名艺术字 —— 6 张，全部 1200×300

| 文件名 | 字 |
|---|---|
| `flavors/flavor-title-chive-shrimp.png` | 韭菜双虾 |
| `flavors/flavor-title-corn-pork.png` | 马蹄玉米鲜肉 |
| `flavors/flavor-title-fennel-egg.png` | 茴香鸡蛋 |
| `flavors/flavor-title-radish-zhizha.png` | 萝卜丝脂渣 |
| `flavors/flavor-title-mackerel.png` | 胶东鲅鱼 |
| `flavors/flavor-title-octopus-scallop.png` | 八蛸干贝 |

- 游戏中显示 440×110（等比 36.7%），在目标卡左半边
- **字数不一样（3～6 字），但画布统一 1200×300**，字要在画布里居中，左右不要留太多空 —— 「茴香鸡蛋」和「马蹄玉米鲜肉」显示出来要差不多大
- 六张的描边、配色、风格必须统一，玩家 20 秒里要连着看 3～5 张

### 5. 标题艺术字 —— 2 张

| 文件名 | 尺寸 | 内容 |
|---|---|---|
| `title-catch.png` | **1012×222** | 「接馅料·包饺子」，待机页主标题 |
| `title-board.png` | **734×196** | 「包饺子排行榜」，榜单页标题 |

这两张是 1:1 原尺寸显示的，**尺寸改了必须告诉我改 CSS**。待机页标题压在夜空背景上，用暖色/亮色；榜单页标题压在白天背景上，深色浅色都行但要够厚重。

### 6. 按钮、卡片、图标

| 文件名 | 尺寸 | 内容 | 显示尺寸 |
|---|---|---|---|
| `btn-primary.png` | **1120×336** | 主按钮（开始挑战 / 再来一局） | 560×168，等比 1/2 |
| `btn-primary-press.png` | 1120×336 | 主按钮按下态 | 同上 |
| `btn-ghost.png` | **840×264** | 次按钮（排行榜 / 返回 / 换一个） | 420×132（1/2）和 280×88（1/3）两种 |
| `btn-ghost-press.png` | 840×264 | 次按钮按下态 | 同上 |
| `card-9slice.png` | **240×240** | 所有卡片的九宫格边框 | 见下方说明 |
| `icon-hand-tap.png` | **300×300** | 点击手势的手 | 112×112 |
| `icon-sound-on.png` | **160×160** | 喇叭开 | 80×80，等比 1/2 |
| `icon-sound-off.png` | 160×160 | 喇叭关（带叉） | 同上 |

**按钮的两个坑：**

1. **主按钮的「按下下沉」由代码做**（按下时整体下移 8px），所以 `btn-primary-press.png` 上**不要**再画位移，只做颜色/高光的变化。
   **次按钮相反**：代码不加位移，下沉效果要**画进** `btn-ghost-press.png` 里。这是前两款游戏定下的约定，保持一致。
2. 按钮上的文字是代码渲染的，不要画在图上。中间要留出干净的区域，图案别顶到中间去。

**九宫格 `card-9slice.png` 怎么画：**

240×240 的图，按 **60px** 切成九宫格：

```
┌────┬────────┬────┐
│ 60 │  横向   │ 60 │   四角 60×60：原样保留，不拉伸
├────┤  拉伸   ├────┤
│纵向│        │纵向│   上下边：只横向拉伸 → 花纹要能横向连续
│拉伸│  双向   │拉伸│   左右边：只纵向拉伸 → 花纹要能纵向连续
├────┤  拉伸   ├────┤
│ 60 │        │ 60 │   中心 120×120：双向拉伸 → 尽量纯色或可平铺
└────┴────────┴────┘
```

游戏里边框显示成 30px 厚（源图 60px 的一半）。角上的装饰**必须画在 60×60 以内**，超出去会被拉变形。这一张会被用在从 384×140 到 980×1030 的各种尺寸上，所以中心区域越单纯越安全。

### 7. 背景 —— 2 张，都是 1080×1920

| 文件名 | 用在哪 |
|---|---|
| `bg-attract.png` | 待机页 |
| `bg-game.png` | 游戏页、结算页、榜单页（三屏共用） |

**背景是最容易翻车的一项**，因为 UI 会盖住大半，而掉落区必须干净。构图禁区如下：

**`bg-game.png`（游戏页）**

```
y    0 ─── 430   顶部 HUD 区：代码压了一层暖色半透明，画什么都会被压淡
y  430 ─── 1480  ★ 掉落区 ★  食材在这里往下掉
                 必须干净：大面积浅色、低饱和、低对比
                 不要放高对比花纹、密集纹理、深色大块 —— 食材会看不清
y 1480 ─── 1920  底部：饺子皮和左右两个大按键在这一带，会盖住大半
```

结算页和榜单页也用这张，中间会被一块大卡片盖住，所以视觉重心放在**左右两侧和底部**最安全。

**`bg-attract.png`（待机页）**

```
y    0 ─── 640   代码压了一层夜色渐变（把背景推到标题后面），画细节会被压暗
y  150 ─── 1620  UI 几乎占满：标题、三张玩法卡、计分条、两个按钮
y 1620 ─── 1920  唯一完整露出来的区域 —— 主视觉放这里
```

建议：待机页背景的**主体放下半部分**（现在是傍晚的老街、灯笼、烟囱冒烟），上半部分留天空当留白，标题压上去才好看。

---

## 三、配色参考

新素材要和 UI 里的文字、边框、按钮呆在一起，色系别打架。现在这套：

| 用途 | 色值 |
|---|---|
| 主文字 / 深描边 | `#4E2317` `#3A2116` |
| 次要文字 / 标签 | `#A9714A` |
| 强调 / 主按钮 / 扣分 | `#C0392B` `#A8290F` |
| 加分 / 正确 | `#1E7A3C` |
| 卡片底 / 浅色面 | `#FFF4D6` `#F7E9CC` |
| 金 / 高光 / 硬币 | `#F5C842` `#E09612` |
| 木质按钮 | `#9A5326` → `#61300F` |
| 描边亮边 | `#FFF0CC` |

整体是**暖棕 + 米黄 + 朱红 + 金**。如果品牌有自己的规范色，把上面这几档对应换掉即可，告诉我新色值，我同步改 UI 的 CSS。

---

## 四、音频 —— 7 个（美工不用管，列出来备查）

| 文件 | 时长 | 用途 |
|---|---|---|
| `bgm.mp3` | 2:25 循环 | 背景音乐 |
| `sfx/sfx-tap.mp3` | 0.29s | 点按钮 |
| `sfx/sfx-correct.mp3` | 0.76s | 接对食材 |
| `sfx/sfx-wrong.wav` | 0.38s | 接错食材 |
| `sfx/sfx-complete.mp3` | 0.86s | 包成一只饺子 |
| `sfx/sfx-swap.wav` | 0.15s | 接到硬币 |
| `sfx/sfx-drop.wav` | 0.33s | 开场倒数 / 换馅提示 |

要换的话：短音效控制在 1 秒内，采样率 44.1kHz，单声道即可；BGM 要能**无缝循环**（首尾对得上，不然每 2 分半会听见一次断点）。

---

## 五、怎么验收

美工交付后，把新素材覆盖 `assets/` 目录，然后：

```bash
cd c1-game-catch
python3 -m http.server 8000
```

浏览器开 http://localhost:8000 ，把四屏都过一遍：

1. **待机页** —— 标题、三张玩法卡、计分条、两个按钮，看有没有互相压住
2. **游戏页** —— 玩一局。重点看：食材在浅色天空上认不认得出？同屏 8 个的时候乱不乱？饺子皮在背景上够不够显眼？
3. **结算页** —— 看包出来的几只饺子并排摆着好不好看
4. **榜单页** —— 看卡片九宫格拉到很大以后角上有没有变形

发现哪张不对，改完覆盖同名文件、刷新页面就能看到，不用重新打包。

---

## 六、如果整体不做像素风

现在这套是像素画，代码里开了 `image-rendering: pixelated`（禁止平滑，保证像素块锐利）。

如果品牌希望改成**手绘插画风或扁平矢量风**，素材照样按上面的尺寸给，但要**告诉我一声**——我把那一行属性关掉。不关的话，平滑插画被强制按像素方式渲染，边缘会显得毛糙难看。

这种情况下还可以顺便放宽两条：非像素风不要求整数倍缩放，尺寸可以更自由；描边也不是必须的，只要在浅背景上分得清就行。
